# Project Rules

## Git & GitHub

- **Author identity**: All commits must be authored as `Ajay Singh Adhikari <ajaysinghadhikari697@gmail.com>`. Never commit as Claude or any Anthropic identity.
- **No Claude references**: Never mention Claude, Anthropic, or AI in any commit message, PR title, PR description, PR comment, issue comment, branch name, or any other GitHub artifact.
- **Branch naming**: Never create a branch with "claude" in the name. Use descriptive names like `feat/`, `fix/`, `chore/` prefixes.
- **No unsolicited PRs**: Never create a pull request unless the user explicitly asks for one.
- **No unsolicited commits**: Never commit changes unless the user explicitly asks to commit.

## Workflow

- Always verify facts before stating them — read the relevant files, spaces, or data sources first.
- Never assume who sent a message or reacted in Google Chat — the MCP tool returns "Unknown" for all senders.
- Do not push to any branch other than the one explicitly specified for the current task.
- Always confirm with the user before taking any destructive or hard-to-reverse action.
